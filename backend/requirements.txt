# --- Runtime ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1  # passlib 1.7.4 is incompatible with bcrypt >= 4.1
python-multipart>=0.0.6
httpx>=0.26.0
pydantic[email]>=2.5.0   # [email] -> email-validator, required by EmailStr
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
arq>=0.25.0            # async task queue - media pipeline worker (app.workers)
redis>=5.0.0
boto3>=1.34.0          # S3-compatible object storage client (app.services.storage)
botocore>=1.34.0       # boto3 dependency; pinned for Config/signature usage
openai>=1.40.0         # LLM segment detection (app.services.segmentation)

# --- Media pipeline ---
# The api and worker share one image (see backend/Dockerfile), so the
# transcription backend is installed here. faster-whisper is CPU/int8 and
# pulls CTranslate2 (no torch); the model weights download on first use.
# FFmpeg + ffprobe binaries are installed by the Dockerfile runtime stage.
faster-whisper>=1.0.0
yt-dlp>=2024.8.6       # URL import for YouTube / Vimeo / Twitch / TikTok / ... page URLs

# --- Dev / test ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
ruff>=0.3.0
